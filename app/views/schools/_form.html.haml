.form-group
  = f.label :name
  = f.text_field :name, :class => 'form-control', :placeholder => 'Name'

.form-group
  = f.label :short_name
  = f.text_field :short_name, :class => 'form-control', :placeholder => 'School  Code'

.form-group
  = f.label :district_id
  =f.select :district_id, options_from_collection_for_select(@districts, :id, :name, @school.district_id), {include_blank: '- Select -'}, {:class => 'form-control'}

.form-group
  = f.label :address
  = f.text_field :address, :class => 'form-control', :placeholder => 'Description'

.form-group
  = f.label :city
  = f.text_field :city, :class => 'form-control', :placeholder => 'Description'

.form-group
  = f.label :state
  = f.text_field :state, :class => 'form-control', :placeholder => 'Description'

.form-group
  = f.label :zip_code
  = f.text_field :zip_code, :class => 'form-control', :placeholder => 'Description'

.form-group
  = f.label :phone_number
  = f.text_field :phone_number, :class => 'form-control'

.form-group
  = f.label :principals_name
  = f.text_field :principals_name, :class => 'form-control'