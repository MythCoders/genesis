<div class="generalAdminHeading aui-page-header">
  <div class="aui-page-header-inner">
    <div class="aui-page-header-main">
      <ol class="aui-nav aui-nav-horizontal">
        <li>
          <h1>
            Edit Grade
          </h1>
        </li>
      </ol>
    </div>
  </div>
</div>
<div class="aui-page-panel">
  <div class="aui-page-panel-inner">
    <div class="aui-page-panel-content">
      <%= form_for :model, url: grades_path, :html => { :class => 'aui' } do |f| %>

          <%= render :partial => 'common/errors', :locals => {:model => f} %>
          <%= render :partial => 'grades/form', :locals => {:f => f} %>

          <div class="buttons-container">
            <div class="buttons">
              <%= f.submit 'Save', :class => 'button submit' %>
              <%= link_to 'Cancel', grades_path, :class => 'cancel' %>
            </div>
          </div>

      <% end %>
    </div>
  </div>
</div>