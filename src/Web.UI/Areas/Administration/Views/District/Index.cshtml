@model List<District>

@{
    ViewBag.ModuleName = "Districts";
    ViewBag.PageTitle = "All Districts";
}

@(Html.Kendo().Grid<District>()
      .Name("pagingGrid")
      .Columns(cols =>
      {
          cols.NavigationButton(p => p.Id, "Select", Url.Action("Edit")).Title("");
          cols.Bound(p => p.SystemCode).Width(125);
          cols.Bound(p => p.Name);
          cols.Bound(p => p.Address.FirstAddressLine);
      })
      .StandardPagingGrid("District", "Administration")
)

@section scripts {
    @Scripts.Render("~/bundles/kendo")
}

@section headerActions {
    <div class="aui-buttons">
        @Html.ActionLink("Create", "Create", null, new { @class = "aui-button" })
    </div>
}
