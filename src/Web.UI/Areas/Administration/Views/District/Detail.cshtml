@model District

@{
    ViewBag.Module = "Districts";
    ViewBag.Title = Model.Name;
}

<form class="aui">
    @Html.DisplayForModel()
</form>

@section headerActions {
    <div class="aui-buttons">
        @Html.ActionLink("Edit", "Edit", new { id = Model.Id }, new { @class = "aui-button" })
        @Html.ActionLink("Edit", "Edit", new {id = Model.Id}, new {@class = "aui-button"})
    </div>
}